Step to run:
Here we provide steps of running Insect Classifier

The demo contains:
1. The folder ./wavefiles/ contains wav files of insects.
2. place the sound clip to /wavefiles/ directory
3. the classLabel.mat(label of class of sound file) in current directory contains the corresponding wav file names and the mapped classes.

To run the demo:
> make
